#[derive(Default, Eq, PartialEq, Clone)]
pub struct PerceptionScales {
    pub id: i64,
    pub level: i64,
    pub extreme: i64,
    pub high: i64,
    pub moderate: i64,
    pub low: i64,
    pub terrible: i64,
}
